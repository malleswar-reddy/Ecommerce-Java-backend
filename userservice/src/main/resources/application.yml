# Application Name
spring:
  application:
    name: userservice
  
  # PostgreSQL DB Configuration
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: Ramcharan8@
    driver-class-name: org.postgresql.Driver

  
  # JPA Configuration
  jpa:
       hibernate:
        ddl-auto: update
       show-sql: true
       properties:
        hibernate:
           dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
  
jwt:
  secret: sg1WwWhfhtMU5CZrFjtqfRV0DvqspyGZrHSiNPhLdfw=